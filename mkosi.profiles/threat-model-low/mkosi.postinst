#!/bin/bash
# SPDX-License-Identifier: LGPL-2.1-or-later
set -eu

find "$BUILDROOT/usr/lib/repart.d" \
	-maxdepth 1 -type f \
	-regex '.*\(swap\|tmp\|root\).conf' \
	-exec sed --in-place '/Encrypt=tpm2/d' {} \;
