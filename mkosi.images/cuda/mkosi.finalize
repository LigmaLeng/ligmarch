#!/bin/sh
set -eux

mkdir -p "$BUILDROOT/usr/lib/ld.so.conf.d"
mv "$BUILDROOT/etc/ld.so.conf.d/cuda.conf" "$BUILDROOT/usr/lib/ld.so.conf.d/"

mkdir -p "$BUILDROOT/usr/share/factory/etc/profile.d/"
mv "$BUILDROOT/etc/profile.d/cuda.sh" "$BUILDROOT/usr/share/factory/etc/profile.d/"
